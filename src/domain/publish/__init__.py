# -*- coding: utf-8 -*-
"""
发布相关领域规则（与具体页面、控件解耦）。

约定（与「位置」模块同一套道理，便于单视频 / 批量视频 / 图文等多入口复用）：

- **标准字段**：与 ``publish_records`` 及插件 ``metadata`` 对齐的编解码、有效展示文案、
  ``dataclass`` 聚合等，放在本包下独立子包。
- **插件消费**：各平台步骤只依赖标准键与 ``domain`` 内辅助函数，避免重复解析 JSON。

已落地子包：
  - ``location_settings`` — ``poi_info``、视频号空位置策略等。
  - ``work_description`` — 作品简介中 ``#话题`` 解析与粘贴规范化（纯函数，无 Qt）。

页面侧配合 ``src.ui.publish`` 下与各域对应的控件与弹窗。
"""
